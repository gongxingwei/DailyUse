/**
 * ⚠️ Event Handler Registry - DEPRECATED
 *
 * This file depends on Schedule module which is temporarily disabled.
 * See modules/schedule/_DEPRECATED_NEEDS_REFACTOR.md for details.
 */

// Empty stub to satisfy imports
export const setupEventHandlers = () => {
  console.warn('Event handlers are temporarily disabled due to Schedule module refactoring');
};

export const registerEventHandlers = () => {
  console.warn('Event handlers are temporarily disabled due to Schedule module refactoring');
};

/**
 * 测试 domain-server 导出
 */

import { ScheduleTask } from '@dailyuse/domain-server';

console.log('✅ ScheduleTask 导出测试通过！');
console.log('   ScheduleTask 类型:', typeof ScheduleTask);
console.log('   ScheduleTask 名称:', ScheduleTask.name);

process.exit(0);

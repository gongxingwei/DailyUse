{
  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 基础路径：相对路径的起点
    "baseUrl": ".",

    // 输出目录：编译后的 JavaScript 和类型声明文件
    "outDir": "./dist",

    // 源码根目录：限制编译范围
    "rootDir": "./src",

    // 生成输出文件
    "noEmit": false,

    // ============================================================
    // Composite 项目配置（Monorepo 库的核心）
    // ============================================================

    // 启用项目引用模式（支持类型热更新）
    "composite": true,

    // 生成类型声明文件
    "declaration": true,

    // 生成声明文件的 source map
    "declarationMap": true,

    // 生成 JavaScript 的 source map
    "sourceMap": true,

    // ============================================================
    // 库环境配置（Domain Client 包）
    // ============================================================

    // 标准库：包含 ES2020 和浏览器 DOM API
    // 因为是客户端领域逻辑，需要 DOM 类型
    "lib": ["ES2020", "DOM"],

    // 类型定义：包含 Vitest 全局类型（用于单元测试）
    "types": ["vitest/globals"],

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]

      // 依赖的包通过 references 引用
      // TypeScript 会从它们的 dist 目录读取类型
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件
  "include": ["src/**/*"],

  // 排除的文件
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"],

  // ============================================================
  // 项目引用配置（依赖的包）
  // ============================================================

  // Domain Client 依赖的包（启用 composite 后恢复 references）
  "references": [{ "path": "../contracts" }, { "path": "../utils" }]
}

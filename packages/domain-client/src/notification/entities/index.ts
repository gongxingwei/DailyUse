/**
 * Notification 模块实体导出 (Client)
 *
 * 注意：客户端实体实现较为简单，主要用于展示和基本操作
 * 客户端通常不需要完整的子实体实现，直接使用 API 按需加载
 */

// export { NotificationChannelClient } from './NotificationChannelClient'; // 客户端暂不实现，使用 API

// NotificationHistoryClient 通常只需要 DTO，不需要完整实体实现
// 可以直接在聚合根中使用 DTO

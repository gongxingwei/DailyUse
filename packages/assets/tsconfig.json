{
  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 基础路径：相对路径的起点
    "baseUrl": ".",

    // 输出目录：编译后的文件
    "outDir": "./dist",

    // 源码根目录：限制编译范围
    "rootDir": "./src",

    // 生成输出文件
    "noEmit": false,

    // ============================================================
    // Composite 项目配置（Monorepo 库的核心）
    // ============================================================

    // 启用项目引用模式
    "composite": true,

    // 生成类型声明文件
    "declaration": true,

    // 生成声明文件的 source map
    "declarationMap": true,

    // ============================================================
    // 库环境配置（Assets 静态资源包）
    // ============================================================

    // 标准库：仅包含 ES2020
    "lib": ["ES2020"],

    // 类型定义：包含 Vite 客户端类型
    // 用于处理静态资源导入（如图片、音频等）
    "types": ["vite/client"],

    // 模块解析：使用 Bundler 策略
    // 因为资源文件由 Vite 打包工具处理
    "moduleResolution": "Bundler",

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件：所有 src 目录下的文件
  "include": ["src/**/*"],

  // 排除的文件
  "exclude": ["node_modules", "dist"],

  // ============================================================
  // 项目引用配置
  // ============================================================

  // Assets 包是独立的静态资源包，不依赖其他包
  "references": []
}

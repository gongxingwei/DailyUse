{
  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 基础路径：相对路径的起点
    "baseUrl": ".",

    // 输出目录：编译后的 JavaScript 和类型声明文件
    "outDir": "./dist",

    // 源码根目录：限制编译范围
    "rootDir": "./src",

    // 生成输出文件
    "noEmit": false,

    // ============================================================
    // Composite 项目配置（Monorepo 库的核心）
    // ============================================================

    // 启用项目引用模式（支持类型热更新）
    "composite": true,

    // 生成类型声明文件
    "declaration": true,

    // 生成声明文件的 source map
    "declarationMap": true,

    // 生成 JavaScript 的 source map
    "sourceMap": true,

    // ============================================================
    // 库环境配置（Domain Core 包）
    // ============================================================

    // 标准库：仅包含 ES2020（核心领域逻辑，不依赖特定环境）
    "lib": ["ES2020"],

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]

      // 依赖的包（contracts, utils）通过 references 引用
      // TypeScript 会自动从它们的 dist 目录读取类型
      // 不需要配置 paths 指向它们的 src 目录
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件
  "include": ["src/**/*"],

  // 排除的文件
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"],

  // ============================================================
  // 项目引用配置（依赖的包）
  // ============================================================

  // Domain Core 依赖的基础包
  // TypeScript 会：
  // 1. 确保这些包先构建
  // 2. 从它们的 dist 目录读取类型定义
  // 3. 监听它们的变化并触发重新编译
  "references": [{ "path": "../contracts" }, { "path": "../utils" }]
}

{
  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 基础路径：相对路径的起点
    "baseUrl": ".",

    // 输出目录：编译后的 JavaScript 和类型声明文件
    "outDir": "./dist",

    // 源码根目录：限制编译范围
    "rootDir": "./src",

    // 生成输出文件
    "noEmit": false,

    // ============================================================
    // Composite 项目配置（Monorepo 库的核心）
    // ============================================================

    // 启用 composite（支持跨包类型热更新）
    // 注意：composite 不影响 tsup 打包，只用于 tsc 类型检查
    "composite": true,

    // 启用增量编译（提升性能）
    "incremental": true,

    // 生成类型声明文件
    "declaration": true,

    // 生成声明文件的 source map
    "declarationMap": true,

    // 生成 JavaScript 的 source map
    "sourceMap": true,

    // ============================================================
    // 库环境配置（Domain Server 包）
    // ============================================================

    // 标准库：仅包含 ES2020（不包含 DOM）
    "lib": ["ES2020"],

    // 类型定义：包含 Node.js 和 Vitest 类型
    "types": ["node", "vitest/globals"],

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]

      // 依赖的包通过 references 引用
      // TypeScript 会从它们的 dist 目录读取类型
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件 - 包含所有 TypeScript 文件
  "include": ["src"],

  // 排除的文件
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"],

  // ============================================================
  // 项目引用配置（依赖的包）
  // ============================================================

  // 注意：使用 tsup 时，references 会导致问题
  // TypeScript 会通过 node_modules 自动解析依赖包的类型
  "references": [{ "path": "../contracts" }, { "path": "../utils" }]
}
